name: wakeed_app
description: وكيد — سند حوالة. تطبيق أندرويد لتسجيل سندات الحوالة عبر منصة وكيد.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  connectivity_plus: ^6.1.4
  flutter_local_notifications: ^19.4.0
  http: ^1.4.0
  intl: any
  path_provider: ^2.1.5
  provider: ^6.1.5
  share_plus: ^11.0.0
  shared_preferences: ^2.5.3
  uuid: ^4.5.1
  wakelock_plus: ^1.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
