{
  "name": "wakeed-remittance-bulk-import",
  "version": "1.0.0",
  "private": true,
  "description": "Bulk paste import for Wakeed remittance vouchers (سندات الحوالات)",
  "scripts": {
    "dev": "concurrently -n server,web -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix server",
    "lint": "npm run lint --prefix web"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/loorksy/wakeed.git"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
